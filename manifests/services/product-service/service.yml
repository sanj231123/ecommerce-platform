apiVersion: v1
kind: Service

metadata:
  name: product-service
  namespace: ecom

  labels:
    app: product-service

spec:

  selector:
    app: product-service

  ports:
  - name: http
    port: 8083
    targetPort: 8083

  type: ClusterIP
