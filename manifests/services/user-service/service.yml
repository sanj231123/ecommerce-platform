apiVersion: v1
kind: Service

metadata:
  name: user-service
  namespace: ecom

spec:
  selector:
    app: user-service

  ports:
    - protocol: TCP
      port: 5006
      targetPort: 5006

  type: ClusterIP
