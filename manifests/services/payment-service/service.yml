apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: ecom

  labels:
    app: payment-service

spec:

  selector:
    app: payment-service

  ports:
  - name: http
    protocol: TCP
    port: 5007
    targetPort: 5007

  type: ClusterIP
