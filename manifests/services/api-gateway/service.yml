apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  namespace: ecom

  labels:
    app: api-gateway

spec:
  type: NodePort

  selector:
    app: api-gateway

  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30091
