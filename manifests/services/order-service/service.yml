apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: ecom

  labels:
    app: order-service

spec:

  selector:
    app: order-service

  ports:
  - name: http
    port: 8086
    targetPort: 8086

  type: ClusterIP
